{
  "name": "qwoted-slack-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Qwoted Slack bot demo for Slack and Vercel.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "vercel-build": "npm run build",
    "clean": "powershell -NoProfile -Command \"if (Test-Path .next) { Remove-Item .next -Recurse -Force }; if (Test-Path dist) { Remove-Item dist -Recurse -Force }\""
  },
  "dependencies": {
    "@slack/web-api": "^7.8.0",
    "mongodb": "^7.2.0",
    "next": "^16.2.7",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/react": "^19.2.16",
    "@types/react-dom": "^19.2.3",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": "24.x"
  }
}
